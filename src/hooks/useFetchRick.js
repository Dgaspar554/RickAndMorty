import { useEffect, useState } from "react";

export const useFetchRick = (page)  => {
    const [Personajes, setPersonajes] = useState([]);
    
    const getPersonajes = async () => {
        
    }
}